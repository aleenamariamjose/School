public with sharing class Square extends Rectangle {
   public Square(Double sideLength) {
       super(sideLength,sideLength);
      // this.height = sideLength;
      // this.width = sideLength;
   }
}
